# Copy to .env.deploy (gitignored) and fill with the BETA Plesk FTP credentials.
FTP_HOST=ftp.it-academy.uz
FTP_USER=
FTP_PASS=
# Docroot of the beta.it-academy.uz subdomain (Plesk shows this; often / or /httpdocs).
FTP_REMOTE_DIR=/
